import { Box } from "@mui/material";

const defaultDimensions = {
    width: 1920,
    height: 1080
}
export default function Display({ dimensions = defaultDimensions}) {

    return (
        <Box>
            
        </Box>
    );
}